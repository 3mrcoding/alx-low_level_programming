#include <stdio.h>

/**
 * main - Entry point
 * @Phrase - The required phrase to be printed
 * Return: Always 0 (Success)
 */

int main(void)
	{
	char Phrase[] = "\"Programming is like building a multilingual puzzle";

	puts(Phrase);

	return (0);

	}
